'use client';

import { Button } from '@/components/ui/button';

const Newtask = () => {
  return (
    <div className='flex justify-center'>
      <Button className='w-3/4'> + New Task </Button>
    </div>
  );
};

export { Newtask };
